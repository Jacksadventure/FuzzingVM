cmake_minimum_required(VERSION 3.23)
project(ThreadingVM)
set(CMAKE_CXX_STANDARD 23)
add_subdirectory(src)
if(build)
    add_executable(main main.cpp)
    target_link_libraries(main ThreadingVM)
endif()
if(test)
    enable_testing()
    add_subdirectory(tests)
endif()
